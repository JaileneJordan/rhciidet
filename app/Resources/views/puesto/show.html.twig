{% extends 'base.html.twig' %}

{% block body %}
    <h1>Puesto</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ puesto.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ puesto.descripcion }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ puesto.activo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('puesto_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('puesto_edit', { 'id': puesto.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
